/**
 * 中间件：用于校验token是否正确
 */

/* module.exports = options => {
  return async function tokenTest(ctx, next){
    
  }

}; */

// 然后在router请求时，可以直接调用中间件
/* module.exports = app => {
  app.router.get(
    "s",
    "/search",
    app.middleware.uppercase(),
    app.controller.search
  );
} */;
